services:
  mayra-jewelry:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: mayra-jewelry
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      # - ${GOPATH}/pkg/mod:/go/pkg/mod
      - .:/app
